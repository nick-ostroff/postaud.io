import { notImplemented } from "../_stub";

// GET  /api/templates      — list for current org
// POST /api/templates      — create template with nested questions
// See plan/04-api-routes.md §2.
export async function GET()  { return notImplemented("list templates"); }
export async function POST() { return notImplemented("create template"); }
