import { notImplemented } from "../../_stub";

// POST /api/billing/portal — returns Stripe billing portal URL.
export async function POST() { return notImplemented("create billing portal session"); }
