import { notImplemented } from "../../_stub";

// POST /api/billing/checkout — returns Stripe Checkout session URL.
export async function POST() { return notImplemented("create Stripe Checkout session"); }
