import { notImplemented } from "../../../_stub";

// POST /api/integrations/webhook/test — fire a signed test payload at sender's webhook_url.
export async function POST() { return notImplemented("send test webhook"); }
