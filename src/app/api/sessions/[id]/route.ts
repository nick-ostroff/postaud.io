import { notImplemented } from "../../_stub";

// GET    /api/sessions/:id — full session detail
// DELETE /api/sessions/:id — delete recording + transcripts + derived artifacts
// See plan/04-api-routes.md §5.
export async function GET()    { return notImplemented("get session detail"); }
export async function DELETE() { return notImplemented("delete session data"); }
