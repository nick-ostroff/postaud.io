import { notImplemented } from "../../../_stub";

// GET /api/sessions/:id/recording — 302 to signed URL (15m TTL).
export async function GET() { return notImplemented("redirect to signed recording URL"); }
