import { notImplemented } from "../../../_stub";

// GET /api/public/request/:token — landing-page data (no auth).
// See plan/04-api-routes.md §6.
export async function GET() { return notImplemented("resolve token → landing data"); }
